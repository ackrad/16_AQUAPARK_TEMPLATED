using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Required_Datas : MonoBehaviour
{
    // Start is called before the first frame update

    public Transform pool_Position;
    public Transform win_Position;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
